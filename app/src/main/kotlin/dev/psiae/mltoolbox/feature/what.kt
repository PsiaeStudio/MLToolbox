package dev.psiae.mltoolbox.feature

// The Feature module is basically the things that we expose to the user
// The presentation if you will
// `app` module depends on `features` module depends on `shared` module